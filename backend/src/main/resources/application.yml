spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chatdb
    username: postgres     # or chatuser
    password: postgres     # or chatpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
